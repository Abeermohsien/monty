#include "monty.h"
/**
 * f_pint - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void f_pint(stack_t **head, unsigned int counter)
{
	if *head == NULL
		printf("stack is empty");
}
else
head --;
{
printf("%d\n", *head);
}
